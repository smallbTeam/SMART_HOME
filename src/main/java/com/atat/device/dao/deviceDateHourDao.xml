<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atat.device.dao.DeviceDateHourDao">

    <insert id="addDeviceDateHour" parameterType="java.util.Map">
        INSERT INTO device_date_hour
        <trim prefix="(" suffix=")" suffixOverrides=",">
            recordTime,
                <if test="deviceId != null">
                    deviceId,
                </if>
                <if test="value != null">
                    value,
                </if>
                <if test="categoryParameterId != null">
                    categoryParameterId,
                </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            #{deviceDateHourId}
                <if test="deviceId != null">
                    #{deviceId},
                </if>
                <if test="value != null">
                    #{value},
                </if>
                <if test="categoryParameterId != null">
                    #{categoryParameterId},
                </if>
        </trim>
    </insert>

    <update id="updateDeviceDateHourById" parameterType="java.util.Map">
        update device_date_hour
        <trim prefix="set" suffixOverrides=",">
                <if test="deviceId != null">
                    deviceId =#{deviceId},
                </if>
                <if test="value != null">
                    value =#{value},
                </if>
                <if test="categoryParameterId != null">
                    categoryParameterId =#{categoryParameterId},
                </if>
        </trim>
        where recordTime = #{deviceDateHourId}
    </update>

    <select id="selectDeviceDateHourList" parameterType="java.util.Map"
            resultType="java.util.Map">
        select
            deviceId deviceId,
            value value,
            categoryParameterId categoryParameterId,
        recordTime deviceDateHourId
        from device_date_hour
        WHERE
        1=1
        <if test="deviceDateHourId != null">
            and recordTime =#{deviceDateHourId}
        </if>
            <if test="deviceId != null">
                and deviceId =#{deviceId}
            </if>
            <if test="value != null">
                and value =#{value}
            </if>
            <if test="categoryParameterId != null">
                and categoryParameterId =#{categoryParameterId}
            </if>
        ORDER BY recordTime ASC
    </select>

    <delete id="delDeviceDateHourById" parameterType="java.lang.String">
        DELETE FROM  device_date_hour
        where
        recordTime = #{deviceDateHourId}
    </delete>

</mapper>
